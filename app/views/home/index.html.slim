h2 お知らせ

table.table.table-striped.table-hover
  tbody
    - if @user_reviewable_count > 0
      tr
        td = link_to "承認依頼が#{@user_reviewable_count}件届いています", request_grants_path
    - if @user_executable_count > 0
      tr
        td = link_to "作業依頼が#{@user_executable_count}件届いています", executable_requests_path
    - if @flow_not_defined_request_count > 0
      tr
        td = link_to "承認フロー未設定状態の申請が#{@flow_not_defined_request_count}件あります", executable_requests_path
 
= link_to '申請を出す', new_request_path, class: 'btn btn-default'
'
= link_to '代理申請を出す', new_request_path(has_client: true), class: 'btn btn-default'
